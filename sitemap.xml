---
layout: null
sitemap: false
---
<?xml version="1.0" encoding="UTF-8"?>
<!--
  Sitemap for https://ishaan-vohra.github.io
  The stable top-level pages are listed explicitly; blog posts are generated
  automatically from the `blog` collection, so new posts appear here on build.
  Publications and projects are rendered inline on the home page (the projects
  collection has output: false; publications link out to external venues), so
  they have no own internal URLs. The academicpages template/demo pages have
  been removed from the site.
-->
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>{{ '/' | absolute_url }}</loc>
    <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
    <changefreq>monthly</changefreq>
    <priority>1.0</priority>
  </url>
  <url>
    <loc>{{ '/blog/' | absolute_url }}</loc>
    <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
    <changefreq>weekly</changefreq>
    <priority>0.8</priority>
  </url>
  {% assign posts = site.blog | sort: 'date' | reverse %}
  {% for post in posts %}
  <url>
    <loc>{{ post.url | absolute_url }}</loc>
    <lastmod>{{ post.last_modified_at | default: post.date | date_to_xmlschema }}</lastmod>
    <changefreq>yearly</changefreq>
    <priority>0.6</priority>
  </url>
  {% endfor %}
  <url>
    <loc>{{ '/terms/' | absolute_url }}</loc>
    <lastmod>2026-06-08T00:00:00+00:00</lastmod>
    <changefreq>yearly</changefreq>
    <priority>0.3</priority>
  </url>
</urlset>
